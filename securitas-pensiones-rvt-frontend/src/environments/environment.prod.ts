/**
 * Production environment configuration.
 * apiBaseUrl should be updated to the production backend URL.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/rvt/api/v1',
  useMocks: false,
};
