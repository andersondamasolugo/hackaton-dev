/**
 * Development environment configuration.
 * apiBaseUrl points to local backend.
 * useMocks flag enables mock interceptor fallback.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/rvt/api/v1',
  useMocks: false,
};
